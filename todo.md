recreate README and all other docs and contributor md and update the app also the techstack and git push , also create and add favicon.ico logo.png and og-image.png and all pwa images required and git push then bun run deploy if this comes = GitHub access needs a refresh. Open Workspace, reconnect GitHub, and approve repository access., give option everywhere to refresh token also store the token locally and if api cors doent happens try to directly do github api call from frontend(avoide rate limiting and conflicts), you can add link of whic  - with there token, can be generated like [https://github.com/settings/tokens/new](https://github.com/settings/tokens/new) with steps, also allow users to manually paste api token and store it - [SH20RAJ/sketch-flow](https://github.com/SH20RAJ/sketch-flow) also add github link on landing page and sidebar to contribute, also add templates to https://sketchflow.space/app/templates and make all of them work better , then create /help page where explain everything and remove the explainations like Workspace model that you have added on all diff pages and keep the main ui very minimal , also you did a blunder I login using another id and see my projects listed there, there dashboard must be blank 

also create a plan for reverting to specific commit only for a project not for whole workspace by copying the data of the perticular project file to new commit also show preview of the commits done in a list also can share the data link of that commit version of that project from github , also identify if the repo is privete or not , also give option to users to use the app offline and dont sync to github until user says and also where can use the app purely offline


improve ux component /;link place,emts landing page, metadata ..text/copy of the website and fix all git  

also users can create online stores.
